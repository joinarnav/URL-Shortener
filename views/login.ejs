<!DOCTYPE html>
<html lang="en">
    <style>
        body {
            background-color: rgb(62, 61, 61);
            color: azure;
            font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
        }
        form {
            margin-top: 20px;
        }
        label, input, button {
            display: block;
            margin: 10px 0;
        }
        button {
            background-color: #2c6fd3;
            color: white;
            border: none;
            padding: 10px 10px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
            margin: 4px 2px;
            cursor: pointer;
            transition-duration: 0.04s;
        }
        button:hover {
            background-color: white;
            color: black;
            border: 2px solid #4CAF50;
        }
        p{
            color: red;
        }
    </style>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
</head>
<body>
    
    <h1>Login</h1>
    <form action="/user/login" method="post">
        <label > Email</label> <input type="text" required name="email" placeholder="Your Email">
        <label > Password</label> <input type="password" required name="password" placeholder="passWord">
        <button type="submit">Login</button>
        <button onclick="window.location.href='http://localhost:8001/signup'">SignUp</button>

    </form>
    <% if (locals.error!=null) { %>
        <p>Invalid Email or Password</p>
    <% } %>

</body>
</html>